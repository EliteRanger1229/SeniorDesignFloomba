RELAY_HOST_PORT=8080

# Broker connection (required)
MQTT_URL=mqtts://your-broker:8883
MQTT_USER=user
MQTT_PASS=pass

# Optional topic filter (default: drone/+/events)
# MQTT_SUB_TOPIC=drone/+/events

# Optional: if using a custom CA, set the Windows/Linux host path here and
# uncomment the 'volumes' section in docker-compose.yml, and set MQTT_CAFILE=/ca.crt
# Example Windows path (Docker Desktop):
# MQTT_CAFILE_HOST=C:\\path\\to\\ca.crt
# Example Linux path:
# MQTT_CAFILE_HOST=/path/to/ca.crt

